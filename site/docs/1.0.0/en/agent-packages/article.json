{
  "schemaVersion": 1,
  "version": "1.0.0",
  "locale": "en",
  "exportedAt": "2026-04-19T03:44:35.012Z",
  "sourceRef": "eeb6954208751b540f20c0339b09a2881f524ce5",
  "article": {
    "slug": "agent-packages",
    "eyebrow": "Agent",
    "title": "Understand agent packages and workspace installation",
    "summary": "Treat built-in assistants as installable capability bundles, understand how they land in .contextgo, and why runtime directories only project skills.",
    "readingTime": "9 min",
    "updatedAt": "2026-04-14",
    "category": "features",
    "html": "<h2 id=\"what-an-agent-package-is\">What an agent package is</h2>\n<p>In ContextGo, an assistant should not be modeled only as “a preset tied to one runtime.”</p>\n<p>The more accurate model is an <strong>Agent Package</strong>. The package carries reusable capability payload, ContextGo installs it into the workspace, and then projects only the runtime-compatible subset into the selected runtime.</p>\n<p>You can think of a built-in assistant as a bundled installation package that ships with the app.</p>\n<h2 id=\"where-built-in-packages-live\">Where built-in packages live</h2>\n<p>In development, bundled Agent Packages live in the product repository under:</p>\n<pre><code class=\"language-text\">src/process/resources/assistant/\n</code></pre>\n<p>In the packaged desktop app, those resources ship inside the application bundle. Users do not need to install built-in packages separately, but the product still treats them as packages internally.</p>\n<h2 id=\"why-packages-are-decoupled-from-runtimes\">Why packages are decoupled from runtimes</h2>\n<p>ContextGo treats the runtime as the execution backend, not as the owner of the assistant package model.</p>\n<p>This gives the product a cleaner boundary:</p>\n<ul>\n<li>the same assistant can move across runtimes without redefining the package</li>\n<li><code>hooks</code>, <code>commands</code>, and <code>schedules</code> stay under ContextGo control</li>\n<li>external open-source assistant packs can be absorbed and translated into first-party product behavior</li>\n<li>future cloud-delivered assistants can reuse the same package contract</li>\n</ul>\n<h2 id=\"where-the-package-installs-in-a-workspace\">Where the package installs in a workspace</h2>\n<p>After installation, the product-owned workspace state lives under:</p>\n<pre><code class=\"language-text\">.contextgo/\n  skills/\n  hooks/\n  hooks.json\n  commands.json\n  schedules.json\n</code></pre>\n<p>This is the canonical workspace installation root.</p>\n<p>In practice:</p>\n<ul>\n<li><code>.contextgo/skills</code> stores installed skills</li>\n<li><code>.contextgo/hooks/</code> and <code>.contextgo/hooks.json</code> store hook payload and hook selection state</li>\n<li><code>.contextgo/commands.json</code> stores the managed command library</li>\n<li><code>.contextgo/schedules.json</code> stores scheduled task configuration</li>\n</ul>\n<h2 id=\"why-runtime-directories-only-project-skills\">Why runtime directories only project skills</h2>\n<p>Different coding runtimes discover skills through their own native directories, so ContextGo may project workspace skills into locations such as:</p>\n<pre><code class=\"language-text\">.codex/skills\n.claude/skills\n.gemini/skills\n</code></pre>\n<p>Those directories are compatibility views, not the source of truth.</p>\n<p>The default rule is:</p>\n<ul>\n<li>runtimes only receive skill projections</li>\n<li><code>hooks</code>, <code>commands</code>, and <code>schedules</code> remain under <code>.contextgo/</code></li>\n<li>third-party runtime workspace layout does not become the product’s primary model</li>\n</ul>\n<p>This is why ContextGo should not generate a full <code>.claude</code> workspace structure in every project just because one absorbed package originally came from the Claude ecosystem.</p>\n<h2 id=\"the-agent-package-v1-protocol\">The Agent Package v1 protocol</h2>\n<p>ContextGo calls this directory contract <code>Agent Package v1</code>.</p>\n<p>The protocol is intentionally small.</p>\n<h3 id=\"required-entry-point\">Required entry point</h3>\n<p>Each package should have:</p>\n<ul>\n<li><code>agent-package.json</code></li>\n<li><code>AGENTS.md</code></li>\n</ul>\n<p><code>agent-package.json</code> is the machine-readable package manifest.</p>\n<p>It should hold the stable mapping from:</p>\n<ul>\n<li>logical payload</li>\n<li>physical source root</li>\n<li>installed workspace surface</li>\n<li>runtime projection behavior</li>\n<li>package-owned capability metadata when UI or bootstrap logic depends on it\n<ul>\n<li><code>skills.packagedSkillNames</code> declares the package-owned skill set</li>\n<li><code>skills.defaultEnabledSkillNames</code> declares assistant defaults</li>\n<li><code>skills.hidePackageOwnedSkillsFromLibrary</code> hides package-owned skills from the generic library when appropriate</li>\n</ul>\n</li>\n</ul>\n<p><code>AGENTS.md</code> is the human-facing package entry point. It should stay concise instead of turning into a giant manual.</p>\n<p>It should explain:</p>\n<ul>\n<li>what the package is for</li>\n<li>which capabilities it installs</li>\n<li>the hard boundaries of the package</li>\n<li>where deeper guidance lives</li>\n</ul>\n<h3 id=\"recommended-deep-documentation\">Recommended deep documentation</h3>\n<p>Each package should also provide:</p>\n<ul>\n<li><code>docs/</code></li>\n</ul>\n<p>Use that directory for longer references, examples, domain guidance, and other material that should not be loaded eagerly.</p>\n<p>Packages may also declare:</p>\n<ul>\n<li><code>workspaceScaffold</code></li>\n</ul>\n<p>This payload represents starter project-root docs that ContextGo can seed into a linked workspace, such as <code>AGENTS.md</code> plus initial files under <code>docs/</code>, when the workspace does not already expose its own root guidance.</p>\n<h3 id=\"executable-capability-payload\">Executable capability payload</h3>\n<p>A package may also include:</p>\n<ul>\n<li>workspace scaffold templates</li>\n<li><code>skills/</code></li>\n<li><code>hooks/</code></li>\n<li>command seeds</li>\n<li>schedule seeds</li>\n</ul>\n<p>After installation, ContextGo normalizes those capabilities into the <code>.contextgo/</code> workspace shape.</p>\n<h2 id=\"progressive-disclosure\">Progressive disclosure</h2>\n<p>The default writing model for Agent Packages is progressive disclosure.</p>\n<p>Split package content across three layers:</p>\n<ul>\n<li><code>agent-package.json</code>: machine-readable payload mapping</li>\n<li><code>AGENTS.md</code>: short entry point and boundaries</li>\n<li><code>workspaceScaffold</code>: project-root starter docs written during workspace bootstrap when appropriate</li>\n<li><code>docs/</code>: deeper package documentation</li>\n<li><code>skills/*/SKILL.md</code>: task-level executable guidance</li>\n</ul>\n<p>This keeps the entry point readable and makes packages easier to maintain and distribute.</p>\n<h2 id=\"future-cloud-import\">Future cloud import</h2>\n<p>ContextGo is designed so bundled assistants and future cloud-delivered assistants can share the same package contract.</p>\n<p>The source of the package may change, but the installed workspace shape should not.</p>\n<p>The target model is:</p>\n<ul>\n<li>bundled assistants use the same package protocol</li>\n<li>local imports use the same package protocol</li>\n<li>future cloud imports use the same package protocol</li>\n</ul>\n<p>That keeps package distribution separate from runtime-specific filesystem behavior.</p>"
  }
}
