{
  "schemaVersion": 1,
  "version": "1.0.0",
  "locale": "zh",
  "exportedAt": "2026-04-21T03:34:45.104Z",
  "sourceRef": "c2c197d2149323d3c906528b534d9076552cc9f3",
  "article": {
    "slug": "agent-packages",
    "eyebrow": "Agent",
    "title": "理解 Agent Packages 与工作空间安装",
    "summary": "把内置 Assistant 理解为可安装的能力包，理解它如何落到 .contextgo，以及为什么运行时目录只做 skills 投影。",
    "readingTime": "9 分钟",
    "updatedAt": "2026-04-14",
    "category": "features",
    "html": "<h2 id=\"什么是-agent-package\">什么是 Agent Package</h2>\n<p>在 ContextGo 里，一个 Assistant 不应该只被理解成“绑定某个 runtime 的预设”。</p>\n<p>更准确的模型是：Assistant 背后挂着一个 <strong>Agent Package</strong>。这个包描述一组可复用能力，然后由产品层把它安装到工作空间，再把其中一部分能力投影给具体运行时。</p>\n<p>你可以把内置 Assistant 理解成“随应用一起分发的内置安装包”。</p>\n<h2 id=\"内置包现在放在哪里\">内置包现在放在哪里</h2>\n<p>开发态下，内置 Agent Packages 跟着产品仓库一起维护，位于：</p>\n<pre><code class=\"language-text\">src/process/resources/assistant/\n</code></pre>\n<p>打包后的桌面应用会把这些资源一起带进应用资源目录。用户不需要单独下载这些内置包，但它们在产品内部仍然按“包”的概念组织。</p>\n<h2 id=\"为什么要和-runtime-解耦\">为什么要和 runtime 解耦</h2>\n<p>ContextGo 把 runtime 当成执行后端，而不是 Assistant 包的拥有者。</p>\n<p>这样做有几个好处：</p>\n<ul>\n<li>同一个 Assistant 可以切换运行时，不需要换一套包语义</li>\n<li><code>hooks</code>、<code>commands</code>、<code>schedules</code> 这些能力可以由 ContextGo 统一管理</li>\n<li>外部开源助手包可以被吸收、转化，再安装成 ContextGo 自己的产品能力</li>\n<li>未来如果从云端导入助手，也可以复用同一套包协议</li>\n</ul>\n<h2 id=\"工作空间里真正的安装位置\">工作空间里真正的安装位置</h2>\n<p>Assistant 包安装到工作空间后，产品层的主写入位置是：</p>\n<pre><code class=\"language-text\">.contextgo/\n  skills/\n  hooks/\n  hooks.json\n  commands.json\n  schedules.json\n</code></pre>\n<p>这里才是工作空间里的主数据面。</p>\n<p>其中：</p>\n<ul>\n<li><code>.contextgo/skills</code> 存放包安装后的 skills</li>\n<li><code>.contextgo/hooks/</code> 和 <code>.contextgo/hooks.json</code> 存放 Hook 载荷与启用状态</li>\n<li><code>.contextgo/commands.json</code> 存放命令库</li>\n<li><code>.contextgo/schedules.json</code> 存放定时任务配置</li>\n</ul>\n<h2 id=\"为什么-runtime-目录只做-skills-投影\">为什么 runtime 目录只做 skills 投影</h2>\n<p>不同代码 Agent runtime 对 skills 的发现方式不同，所以 ContextGo 会把 workspace 中的 skills 投影到运行时原生目录，例如：</p>\n<pre><code class=\"language-text\">.codex/skills\n.claude/skills\n.gemini/skills\n</code></pre>\n<p>但这些目录只是兼容层，不是安装源。</p>\n<p>ContextGo 的默认规则是：</p>\n<ul>\n<li>runtime 只接收 skills 投影</li>\n<li><code>hooks</code>、<code>commands</code>、<code>schedules</code> 继续留在 <code>.contextgo/</code></li>\n<li>不把第三方 runtime 的工作空间结构直接当成产品主模型</li>\n</ul>\n<p>这也是为什么现在不再鼓励因为某个包来自 Claude 生态，就默认在任意 workspace 里生成整套 <code>.claude</code> 结构。</p>\n<h2 id=\"agent-package-v1-协议\">Agent Package v1 协议</h2>\n<p>ContextGo 把这套安装包目录约定称为 <code>Agent Package v1</code>。</p>\n<p>这个协议故意保持小而清晰。</p>\n<h3 id=\"必需入口\">必需入口</h3>\n<p>每个包应该有：</p>\n<ul>\n<li><code>agent-package.json</code></li>\n<li><code>AGENTS.md</code></li>\n</ul>\n<p><code>agent-package.json</code> 是机器可读的包清单。</p>\n<p>它至少应该稳定描述：</p>\n<ul>\n<li>logical payload</li>\n<li>physical source root</li>\n<li>安装到 workspace 后的目标 surface</li>\n<li>是否需要 runtime projection</li>\n<li>当 UI 或 bootstrap 逻辑依赖包级能力集合时，对应的稳定字段\n<ul>\n<li><code>skills.packagedSkillNames</code> 用来声明包拥有的 skill 集</li>\n<li><code>skills.defaultEnabledSkillNames</code> 只表示助手默认启用项</li>\n<li><code>skills.hidePackageOwnedSkillsFromLibrary</code> 表示这些包内 skill 是否应从通用技能库中隐藏</li>\n</ul>\n</li>\n</ul>\n<p><code>AGENTS.md</code> 是包的入口说明，不应该写成超大手册。</p>\n<p>它应该简短说明：</p>\n<ul>\n<li>这个包适合做什么</li>\n<li>它安装了哪些能力</li>\n<li>有哪些硬边界</li>\n<li>更深的说明放在哪里</li>\n</ul>\n<h3 id=\"推荐的深入文档\">推荐的深入文档</h3>\n<p>每个包建议再提供：</p>\n<ul>\n<li><code>docs/</code></li>\n</ul>\n<p>这里用于放更长的领域说明、示例、注意事项和参考资料。</p>\n<p>包还可以声明：</p>\n<ul>\n<li><code>workspaceScaffold</code></li>\n</ul>\n<p>这个 payload 表示安装时可写入 linked workspace 的项目根 starter docs，例如 <code>AGENTS.md</code> 和 <code>docs/</code> 下的初始文件。前提是目标 workspace 还没有自己的根说明结构。</p>\n<h3 id=\"可执行能力载荷\">可执行能力载荷</h3>\n<p>一个包还可以带：</p>\n<ul>\n<li>workspace scaffold templates</li>\n<li><code>skills/</code></li>\n<li><code>hooks/</code></li>\n<li>命令种子</li>\n<li>定时任务种子</li>\n</ul>\n<p>安装完成后，这些能力会被规范化到 <code>.contextgo/</code> 里。</p>\n<h2 id=\"渐进式披露原则\">渐进式披露原则</h2>\n<p>ContextGo 对 Agent Package 的默认写法是渐进式披露。</p>\n<p>建议按这三层拆开：</p>\n<ul>\n<li><code>agent-package.json</code>：机器可读的 payload 映射</li>\n<li><code>AGENTS.md</code>：短入口，只负责定边界、给导航</li>\n<li><code>workspaceScaffold</code>：在合适时机写入 workspace 根目录的 starter docs</li>\n<li><code>docs/</code>：放更深的文档，不默认全部加载</li>\n<li><code>skills/*/SKILL.md</code>：放任务级执行说明</li>\n</ul>\n<p>这样可以避免把所有内容都塞进一个很长的入口文件，也更适合后续维护和导入。</p>\n<h2 id=\"未来的云端导入\">未来的云端导入</h2>\n<p>ContextGo 未来希望让内置助手和云端导入助手共用同一套包协议。</p>\n<p>也就是说，未来变化的应该只是“包从哪里来”，而不是“安装到 workspace 后长什么样”。</p>\n<p>目标模型是：</p>\n<ul>\n<li>内置助手使用同一协议</li>\n<li>本地导入使用同一协议</li>\n<li>未来云端分发也使用同一协议</li>\n</ul>\n<p>这样后续扩展助手时，不需要每次都围绕某个 runtime 的私有目录重新设计一遍。</p>"
  }
}
